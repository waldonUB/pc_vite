```javascript
	//自定义封装select组件
	/*
 * <select v-model="value" placeholder="啊啊啊啊啊" disabled></select>
		@array,list,数组，用来生成选择的项
		@string，placeholder,没有输入文字时的提示
		@string，disabled,是否能够使用此选择框
		@model, selectVal,绑定父组件的model
		@object, selectkey,父组件使用的key-value的名称是什么。

		to-do:
		1、点击的时候再去生成对应的选择项
		2、动态加载选项
	*/
```
